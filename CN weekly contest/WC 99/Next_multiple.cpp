int nextMultiple(int n, int m) {

    // Write your code here.
   /* int start = n/m;
    if(start == 0) start = 1;
     int ans = 1;

    while(1){
      ans = m*start;
      if(ans>=n) return ans;
      start++;
    }
    */
    //  return (n/m + 1)*m;
    // return n + (m-n%m);
    

}